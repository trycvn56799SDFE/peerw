apiVersion: v1
kind: Service
metadata:
  name: peerw
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "peerw"
    port: 8080
  selector:
    app: peerw
